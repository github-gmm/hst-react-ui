import { Space } from 'antd';
import { HstButton } from 'hst-react-ui';
import React from 'react';

export default () => {
  return (
    <Space wrap>
      <HstButton variant="link">primary color</HstButton>
      <HstButton variant="link" color="default">
        default color
      </HstButton>
      <HstButton variant="link" color="green">
        green color
      </HstButton>
      <HstButton variant="link" color="red">
        red color
      </HstButton>
      <HstButton variant="link" color="orange">
        orange color
      </HstButton>
      <HstButton variant="link" color="blue">
        blue color
      </HstButton>
      <HstButton variant="link" color="geekblue">
        geekblue color
      </HstButton>
      <HstButton variant="link" color="cyan">
        cyan color
      </HstButton>
      <HstButton variant="link" color="magenta">
        magenta color
      </HstButton>
      <HstButton variant="link" color="pink">
        pink color
      </HstButton>
      <HstButton variant="link" color="yellow">
        yellow color
      </HstButton>
      <HstButton variant="link" color="volcano">
        volcano color
      </HstButton>
      <HstButton variant="link" color="lime">
        lime color
      </HstButton>
      <HstButton variant="link" color="gold">
        gold color
      </HstButton>
    </Space>
  );
};
